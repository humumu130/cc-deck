{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口最小暴露面：core:default 打通 IPC 通道（应用自有命令 probe_local 不在 ACL 管控范围，无远程页面可复用本能力）",
  "windows": ["main"],
  "permissions": ["core:default"]
}
